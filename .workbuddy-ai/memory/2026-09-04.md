- 用户确认 NewAPI 任务最终制品列表的实际接口为 `GET /v1/tasks/:task_id/artifacts`，不是 `/api/task/:task_id/artifacts`；视频内容接口相应为 `/v1/tasks/:task_id/artifacts/:key/content`。后续插件调试与文档说明统一以该宿主通用 artifact 路由为准。
- 用户明确要求将这些跨厂商 artifact 通配解析技巧沉淀到项目 Skill；已将规则写入 `new-api-task-plugin-builder` 的通用 Artifact 适配章节，后续可复用到其他任务插件。
- 完成火山引擎方舟 3D 生成插件（`volcengine-ark-3d` v1.0.0）：覆盖 Seed3D、Hyper3D-Gen2、HiTem3D 三个模型系列，采用原生同构适配，保留厂商扩展字段和 `false/0/null/空数组`；实现六层状态阶梯（envelope 错误、文档枚举、前缀模糊、结果 URL、失败信号、安全默认 `QUEUED`）、artifact 发现与 credentialless 回源、用量跟踪（Hyper3D 30000 token/次，其他 1 token/次）。分配 `channelTypes: [10001]`，已登记到 Skill；合同测试全覆盖（isomorphic 保真、状态映射、artifact、renderer）。
- 完成火山引擎豆包异步长文本 TTS 插件（`volcengine-tts-async` v1.0.0，`channelTypes: [10002]`）：接入 openspeech `api/v3/tts` 大模型异步接口（seed-tts-2.0 / seed-icl-2.0），原生同构直通。关键差异（已沉淀到 Skill 新增「火山引擎豆包异步 TTS 适配经验」章节）：成功码是 `20000000` 非 0；模型版本走 `X-Api-Resource-Id` header 不进 body；鉴权用 `X-Api-Key` 非 Bearer；query 是 POST+body `{task_id}` 非 GET path；v3 `task_status` 数字枚举无官方表且与 v1 语义相反（v1: 0中/1成/2败），故用 `audio_url`+成功码作权威成功信号、数字枚举仅当进度提示。artifact 为音频（bytespeech 1h 签名 URL，credentialless 回源），用量取 `synthesize_text_length`。53 项合同测试全通过。
- 完成火山引擎录音文件识别标准版插件（`volcengine-asr-auc` v1.0.0，channelType 10003）：同构直通 `api/v3/auc/bigmodel/submit|query`，模型 `volc.seedasr.auc`/`volc.bigasr.auc`。关键点：①任务状态在 **HTTP 响应头 `X-Api-Status-Code`**（20000000 成功/20000001 处理中/20000002 队列/20000003 静音/45xx 参数/55xx 服务端），宿主 `parseTaskResult(ctx, body, response)` 第三参数 `response:{status,headers}` 提供响应头（`HookHTTPResponse`）；②task ID 由客户端生成经 `X-Api-Request-Id` 头回传，提交配 `X-Api-Sequence:-1`，查询 body 为空 `{}`、method 仍 POST；③模型版本走 `X-Api-Resource-Id` 头、不进 body；④产物是识别文本 `result.text`、非媒体文件，无 artifact，原生 query presenter 透传；⑤用量按 `audio_info.duration` 毫秒向上取整秒，`extractUsageOnComplete`。49 条合同断言全通过。已把「响应头状态/客户端 task ID 头回传/纯文本产物」三类通用经验沉淀进 Skill 第 2 节，并补登 10002(tts)/10003(asr) 到 channelTypes 表。
- 确认宿主插件合同权威来源：`QuantumNous/new-api` 仓库 `docs/plugin-api/{v1.md,v1.d.ts,README.md}`；`parseTaskResult`/`parseBatchResult` 收 `{status, headers}` 第三参，`UpstreamResponse.headers` 是 `Record<string,string[]>`（数组），`HookHTTPResponse.headers` 是 `Record<string,string>`。
- 用户明确原生路由路径命名约定：必须保真保留厂商原生路径，只在最前加业务命名空间 `/{biz}`（如 `/volcengine` + `/api/v3/contents/generations/tasks`）；顶多再插入**单个** `/{type}` 段区分任务类型（`/volcengine/video/api/v3/...`），不得改写/精简厂商原始 path 段，不得自造 `/vendor/v1/jobs` 简写。已把该规则写入 Skill「原生路由设计」新增「路径命名约定（硬性要求）」章节并替换示例。
- 按新路径约定重构三个已适配的 volcengine 插件路由（各出 1.0.1 patch，1.0.0 目录保留不动）：ark-3d `/ark/3d/generations/tasks` → `/volcengine/3d/api/v3/contents/generations/tasks`（因上游 `/api/v3/contents/generations/tasks` 是 Ark 通用内容端点、与 video/image 共用，故加 `/3d` type 段区分）；tts-async `/volc/tts/v3/submit|query` → `/volcengine/api/v3/tts/submit|query`；asr-auc `/volc/auc/v3/submit|query` → `/volcengine/api/v3/auc/bigmodel/submit|query`。仅改 `meta.version`+`meta.routes[].path`，上游 URL 与业务逻辑不动，channelTypes（10001/10002/10003）保持不变避免用户重绑渠道。三者 `node --check` 通过，ark-3d/asr-auc 合同测试全绿（asr 49 断言）。

# 2026-09-04

## WAND-Vega 插件创建

创建了腾讯云 WAND-Vega 图像生成插件 `wand-vega/1.0.0`。

### 核心信息
- **插件 key**: `wand-vega`
- **版本**: `1.0.0`
- **channelTypes**: `[10004]`（已分配，当前最大值为 10003）
- **模型支持**: 
  - `wand-vega-image-lite` - 高性价比
  - `wand-vega-image-flash` - 平衡型
  - `wand-vega-image-pro` - 高质量

### 功能实现
1. **文生图** (Text-to-Image) - 纯文本提示词生成图像
2. **参考生图** (Image-to-Image) - 支持 0-6 张参考图（Lite 最多 3 张）
3. **异步任务模式** - 提交任务 → 轮询结果（建议 3-5 秒间隔）
4. **动态用量提取** - 任务完成后从 `usage.total_tokens` 获取实际消耗

### 状态映射
按文档声明式枚举精确映射：
- `completed/success/succeeded` → `SUCCESS`
- `in_progress/processing/running` → `IN_PROGRESS`
- `queued/pending/submitted` → `QUEUED`
- `failed/failure/cancelled/expired` → `FAILURE`

### API 端点
- **提交**: `POST /v1/wand/vega/images/generations`
- **查询**: `GET /v1/wand/vega/images/tasks/:task_id`

### 协议支持
- `openai_responses` - 支持 sync/background 模式
- Native 端点 - 原生 WAND API 格式

### 验证结果
✅ ES6 模块语法检查通过
✅ Hook 导出完整性检查通过
✅ 文生图功能测试通过
✅ 参考生图功能测试通过
✅ 状态解析测试通过
✅ OpenAI 协议适配测试通过

### 文件清单
- `plugins/tasks/wand-vega/1.0.0/plugin.js` - 插件主文件
- `plugins/tasks/wand-vega/README.md` - 插件文档
