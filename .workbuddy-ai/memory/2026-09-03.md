- 新增 `plugins/tasks/minimax-h3/1.0.0/plugin.js`：基于上游 Hailuo 插件抽取 MiniMax-H3 专用实现，支持 `/v2/video_generation`、任务查询、H3 多模态 content、4-15 秒、768P/2K、比例校验、流式/同步 OpenAI Responses 与 OpenAI Video 协议。
- 后续补充 `meta.routes` 与 `native`，开放插件原生端点：POST `/minimax-h3/v2/video_generation`、GET `/minimax-h3/v2/query/video_generation/:task_id`，并完成原生端点合同测试。
- 已通过 Node 模块加载、JavaScript 语法和核心请求/响应测试；尚未生成仓库 `index.json`，因当前工作区没有相邻 new-api 编译工具。
- 新增 `plugins/tasks/kling-v3-turbo/1.0.0/plugin.js`：将 TokenHub Kling 3.0 Turbo 图生视频 `contents/settings/options` 请求转换为原生 Kling `/v1/videos/image2video` 格式，支持异步任务查询、状态/视频 URL 归一化、artifact 下载和用量提取；已通过 Node 语法及 smoke tests。
- 针对用户提供的 TokenHub 文生、图生、首尾帧和任务查询示例完成本地适配测试；修复了纯文生默认时长常量遗漏、文生路由选择和统一 `/v1/tasks/:task_id` 查询路径，三类请求均可正确转换且未使用用户 API Key 发起外部请求。
- 按用户给出的真实原生路径重新校准路由：提交端点固定为 `https://newapi-bff.oneapis.cn/kling/image-to-video/kling-3.0-turbo` 这类 `/kling/{operation}/{model}` 形式，查询固定为 `/kling/v1/tasks/:task_id`；本地 smoke test 已确认当前 `baseUrl=https://newapi-bff.oneapis.cn/kling` 时生成目标路径。
- 将 README 中的仓库约定写入技能 `new-api-task-plugin-builder`：新增「仓库约定（硬性要求）」章节（author 固定 `Jushenzhidao`、channelTypes 从 10000 递增、version 从 1.0.0 起）、channelTypes 分配流程与末尾登记表，并同步更新「插件骨架」「验证要求」及 frontmatter description。
- 调查 TokenHub 返回 `service ID does not exist, or model does not match service`：确认插件曾错误把 TokenHub 的模型映射为原生模型并丢失 `model`；现已改为向 TokenHub 原样发送 `model: kling-video-v3` 等 TokenHub 模型名和 `contents/settings/options`，并在路由层去除空模型硬匹配；本地验证请求体模型正确。
- 优化技能 `new-api-task-plugin-builder` 的「状态与结果」章节：确立上游→new-api 状态映射六层阶梯（0 envelope 错误码 → 1/2 文档声明式+精确枚举表 `STATUS_TABLE`/`STATUS_MAP` → 3 前缀模糊 `success|succ|ok|okay|comp*` 成功、`erro*|fail*|cancel*|expire*|timeout*|reject*|abort*` 失败、`run*|process*|progress*|generat*|render*` 进行中、`queue*|pend*|submit*|wait*|not_start` 排队 → 4 结果字段 `url/video_url/image_url/data[].url` 兜底成功 → 5 失败信号 `fail_reason/err_msg/error.message` 兜底失败 → 6 默认 `QUEUED`，永不返回 `UNKNOWN`）。硬规则：模糊层只许前缀匹配（禁子串，防 `not_completed` 误判）、结果兜底让位于失败信号、URL 须过 `^https?://`。参考实现已抽取跑通 26 例合同测试。已同步更新调试决策树第 4 条与验证要求的逐层单测清单。
- 为技能新增「插件内结构化日志」章节：插件在宿主 JS 沙箱里只有 `console.log` 一个可观测出口，规定单行 JSON、字段白名单（`logStr`/`logNum` 先把每个字段收敛成标量，避免循环引用让 `JSON.stringify` 抛错丢整行）、`typeof console` 守卫 + `try/catch`、URL 只留 origin+path 去掉 query、禁打 apiKey/Authorization/请求体、轮询按状态跃迁或 `shouldLog` 冷却降频（Map 超 512 清空）、`status_unrecognized` 作为补 `STATUS_TABLE` 的信号源。参考实现跑了 14 例冒烟测试全过；过程中修掉一处真实缺陷——白名单字段传入对象会导致整条日志丢失。同步更新调试决策树（新增第 0 条「先按 taskId 拉日志」、第 6 条改为只记字段形状）与验证要求的日志静态检查项。
- 修复 TokenHub Kling 轮询成功但 NewAPI 一直排队的问题：实际查询响应为 `data: [{ status: "succeeded", outputs: [{ type: "video", url: "..." }] }]` 数组结构，插件此前把数组当对象导致状态为空并归一化为 QUEUED；1.0.1 现已取 `data[0]` 并从 `outputs[].url` 提取视频地址，已用真实结构模拟测试通过。
- 新增 Kling 结构化日志测试版 `plugins/tasks/kling-v3-turbo/1.0.2/plugin.js`（由 1.0.1 新建 patch，保留 author/channelType）：模块加载打 `plugin_loaded` + `sandbox_probe`，并覆盖 `decode_start`、`submit_start/request/error/done/failed`、`query_request`、`task_status`、`status_unrecognized`。每条单行 JSON 带固定 `source:new-api-task-plugin`、plugin/version/event；URL 去 query、字段白名单标量化、不记录 apiKey/Authorization/prompt/body；轮询日志 60 秒冷却。已通过语法、模块加载、协议解码、提交/查询/状态、错误路径、未知状态和敏感信息不泄露测试。
- 继续优化 `new-api-task-plugin-builder` 技能：新增"本地正常但线上超时"的日志证据链诊断。规定收到用户日志先分析；区分无插件标识（版本未加载或 console 未收集）、有加载探针但无 decode/submit（请求未进入插件执行路径）、有 submit_request 无 submit_done/failed（插件已构造请求，但宿主网络发送阶段异常，转查 Go HTTP/DNS/TLS/代理/防火墙）、有响应事件（上游已返回）。要求结论必须指出事件链断点，不能以本地测试代替线上链路证据。
- 查上游 new-api 文档确认日志不输出的根因：宿主**只在 `DEBUG=true` 时才转发插件 `console.log`**（文档原文 "Plugin console.log output is also forwarded in DEBUG mode"）。生产环境默认不开 DEBUG，插件日志一条都不会出现——这不是插件 bug。已在技能「插件内结构化日志」开头加 ⚠️ 提示，在调试决策树第 0.2 步把"先查 DEBUG=true"列为头号排查项，在「沙箱兼容提醒」补充宿主转发条件和 `task_plugin` 过滤关键字。同步更新 MEMORY.md。
- 因生产环境无法打印插件日志，从技能中删除整个「插件内结构化日志」章节（含硬性约束、埋点位置清单、参考实现、沙箱兼容提醒）以及验证要求中的日志静态检查、日志冒烟和线上超时证据链条目。删除为测试日志而创建的 `kling-v3-turbo/1.0.2` 目录。调试决策树第 0 条改为：线上排障不依赖插件日志，改用宿主 Go 层 HTTP 日志和上游 access log 定位；仅保留一句话说明 DEBUG 模式下才可能获得插件日志。同步精简 MEMORY.md 的日志约定。
- 新建 Kling `1.0.8`，按用户要求让原生查询 renderer 透传 TokenHub 完整成功响应，并额外补充公开 `id/task_id/status/url` 字段；对真实 `data[]/outputs[]` 结构完成本地测试，URL 非空且状态 succeeded。
